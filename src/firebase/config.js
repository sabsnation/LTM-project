// Este arquivo foi substituído por init.js
// Mantendo este arquivo apenas por compatibilidade se necessário
// Agora apenas exporta o que está em init.js para evitar duplicação

import app from './init'

export default app